@{
    @* point Identity pages to the Identity-specific layout, NOT the main site layout *@
    @* the main layout has basket counts, auth checks and tag helpers that break on login pages *@
    Layout = "/Areas/Identity/Pages/_Layout.cshtml";
}
