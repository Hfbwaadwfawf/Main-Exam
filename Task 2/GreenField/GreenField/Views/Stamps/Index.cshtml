@model IEnumerable<GreenField.Models.Stamps>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StampName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StampDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StampName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StampDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StampsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StampsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StampsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
