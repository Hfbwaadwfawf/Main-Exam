@model IEnumerable<GreenField.Models.ProducerStamps>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Producers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stamps)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Producers.ProducersId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stamps.StampsId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProducerStampsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProducerStampsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProducerStampsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
